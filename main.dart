// lib/main.dart

import 'package:flutter/material.dart';
import 'app.dart'; // Import the app's root widget.

void main() {
  // This line starts the entire application.
  runApp(const CompanyApp());
}